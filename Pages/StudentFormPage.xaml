<Page x:Class="pract12.Pages.StudentFormPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:pract12.Pages"
      xmlns:services="clr-namespace:pract12.Service"
      xmlns:validation="clr-namespace:pract12.ValidationRules"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="800" d:DesignWidth="800"
      Title="Форма студента"
      Background="{DynamicResource MaterialDesignBackground}">

    <Page.Resources>
        <services:RolesService x:Key="rolesService"/>
    </Page.Resources>

    <Grid>
        <materialDesign:Card Margin="24" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" 
                        Background="{DynamicResource MaterialDesignToolBarBackground}"
                        Padding="24,16">
                    <TextBlock x:Name="FormTitle" 
                             Text="Добавление студента" 
                             FontSize="20"
                             FontWeight="Medium"
                             Foreground="{DynamicResource MaterialDesignBody}"/>
                </Border>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="24">
                    <StackPanel Width="400">

                        <StackPanel Margin="0,0,0,16">
                            <TextBlock Text="Логин *" FontSize="14" FontWeight="Medium" Margin="0,0,0,4"
                                     Foreground="{DynamicResource MaterialDesignBody}"/>
                            <TextBox x:Name="LoginTextBox"
                                     materialDesign:HintAssist.Hint="Минимум 5 символов">
                                <TextBox.Text>
                                    <Binding Path="Login" UpdateSourceTrigger="PropertyChanged">
                                        <Binding.ValidationRules>
                                            <validation:RequiredRule/>
                                            <validation:MinLengthRule MinLength="5"/>
                                        </Binding.ValidationRules>
                                    </Binding>
                                </TextBox.Text>
                            </TextBox>
                            <ItemsControl Foreground="{DynamicResource MaterialDesignValidationErrorBrush}" FontSize="12"
                                        ItemsSource="{Binding ElementName=LoginTextBox, Path=(Validation.Errors)}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding ErrorContent}" TextWrapping="Wrap" Margin="4,2,0,0"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,16">
                            <TextBlock Text="Имя *" FontSize="14" FontWeight="Medium" Margin="0,0,0,4"
                                     Foreground="{DynamicResource MaterialDesignBody}"/>
                            <TextBox x:Name="NameTextBox"
                                     materialDesign:HintAssist.Hint="Введите полное имя">
                                <TextBox.Text>
                                    <Binding Path="Name" UpdateSourceTrigger="PropertyChanged">
                                        <Binding.ValidationRules>
                                            <validation:RequiredRule/>
                                        </Binding.ValidationRules>
                                    </Binding>
                                </TextBox.Text>
                            </TextBox>
                            <ItemsControl Foreground="{DynamicResource MaterialDesignValidationErrorBrush}" FontSize="12"
                                        ItemsSource="{Binding ElementName=NameTextBox, Path=(Validation.Errors)}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding ErrorContent}" TextWrapping="Wrap" Margin="4,2,0,0"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>

                     
                        <StackPanel Margin="0,0,0,16">
                            <TextBlock Text="Email *" FontSize="14" FontWeight="Medium" Margin="0,0,0,4"
                                     Foreground="{DynamicResource MaterialDesignBody}"/>
                            <TextBox x:Name="EmailTextBox"
                                     materialDesign:HintAssist.Hint="example@domain.com">
                                <TextBox.Text>
                                    <Binding Path="Email" UpdateSourceTrigger="PropertyChanged">
                                        <Binding.ValidationRules>
                                            <validation:RequiredRule/>
                                            <validation:EmailRule/>
                                        </Binding.ValidationRules>
                                    </Binding>
                                </TextBox.Text>
                            </TextBox>
                            <ItemsControl Foreground="{DynamicResource MaterialDesignValidationErrorBrush}" FontSize="12"
                                        ItemsSource="{Binding ElementName=EmailTextBox, Path=(Validation.Errors)}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding ErrorContent}" TextWrapping="Wrap" Margin="4,2,0,0"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,16">
                            <TextBlock Text="Пароль *" FontSize="14" FontWeight="Medium" Margin="0,0,0,4"
                                     Foreground="{DynamicResource MaterialDesignBody}"/>
                            <TextBox x:Name="PasswordTextBox"
                                     materialDesign:HintAssist.Hint="Минимум 8 символов: A-Z, a-z, 0-9, спецсимволы">
                                <TextBox.Text>
                                    <Binding Path="Password" UpdateSourceTrigger="PropertyChanged">
                                        <Binding.ValidationRules>
                                            <validation:RequiredRule/>
                                            <validation:PasswordRule/>
                                        </Binding.ValidationRules>
                                    </Binding>
                                </TextBox.Text>
                            </TextBox>
                            <ItemsControl Foreground="{DynamicResource MaterialDesignValidationErrorBrush}" FontSize="12"
                                        ItemsSource="{Binding ElementName=PasswordTextBox, Path=(Validation.Errors)}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding ErrorContent}" TextWrapping="Wrap" Margin="4,2,0,0"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>

                       
                        <StackPanel Margin="0,0,0,16" Visibility="Collapsed">
                            <TextBlock Text="Дата создания" FontSize="14" FontWeight="Medium" Margin="0,0,0,4"
                                     Foreground="{DynamicResource MaterialDesignBody}"/>
                            <DatePicker x:Name="DatePicker" IsEnabled="False">
                                <DatePicker.SelectedDate>
                                    <Binding Path="CreatedAt" UpdateSourceTrigger="PropertyChanged">
                                        <Binding.ValidationRules>
                                            <validation:DateRule/>
                                        </Binding.ValidationRules>
                                    </Binding>
                                </DatePicker.SelectedDate>
                            </DatePicker>
                            <ItemsControl Foreground="{DynamicResource MaterialDesignValidationErrorBrush}" FontSize="12"
                                        ItemsSource="{Binding ElementName=DatePicker, Path=(Validation.Errors)}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding ErrorContent}" TextWrapping="Wrap" Margin="4,2,0,0"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>

                       
                        <StackPanel Margin="0,0,0,16">
                            <TextBlock Text="Ссылка на изображение" FontSize="14" FontWeight="Medium" Margin="0,0,0,4"
                                     Foreground="{DynamicResource MaterialDesignBody}"/>
                            <TextBox x:Name="AvatarUrlTextBox"
                                     materialDesign:HintAssist.Hint="URL аватара">
                                <TextBox.Text>
                                    <Binding Path="UserProfile.AvatarUrl" UpdateSourceTrigger="PropertyChanged">
                                        <Binding.ValidationRules>
                                            <validation:UrlRule/>
                                        </Binding.ValidationRules>
                                    </Binding>
                                </TextBox.Text>
                            </TextBox>
                            <ItemsControl Foreground="{DynamicResource MaterialDesignValidationErrorBrush}" FontSize="12"
                                        ItemsSource="{Binding ElementName=AvatarUrlTextBox, Path=(Validation.Errors)}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding ErrorContent}" TextWrapping="Wrap" Margin="4,2,0,0"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>

                       
                        <StackPanel Margin="0,0,0,16">
                            <TextBlock Text="Номер телефона" FontSize="14" FontWeight="Medium" Margin="0,0,0,4"
                                     Foreground="{DynamicResource MaterialDesignBody}"/>
                            <TextBox x:Name="PhoneTextBox"
                                     materialDesign:HintAssist.Hint="+7 (XXX) XXX-XX-XX">
                                <TextBox.Text>
                                    <Binding Path="UserProfile.Phone" UpdateSourceTrigger="PropertyChanged">
                                        <Binding.ValidationRules>
                                            <validation:PhoneRule/>
                                        </Binding.ValidationRules>
                                    </Binding>
                                </TextBox.Text>
                            </TextBox>
                            <ItemsControl Foreground="{DynamicResource MaterialDesignValidationErrorBrush}" FontSize="12"
                                        ItemsSource="{Binding ElementName=PhoneTextBox, Path=(Validation.Errors)}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding ErrorContent}" TextWrapping="Wrap" Margin="4,2,0,0"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>

                    
                        <StackPanel Margin="0,0,0,16">
                            <TextBlock Text="Дата рождения" FontSize="14" FontWeight="Medium" Margin="0,0,0,4"
                                     Foreground="{DynamicResource MaterialDesignBody}"/>
                            <DatePicker x:Name="BirthdayDatePicker"
                                        materialDesign:HintAssist.Hint="ДД.ММ.ГГГГ">
                                <DatePicker.SelectedDate>
                                    <Binding Path="UserProfile.Birthday" UpdateSourceTrigger="PropertyChanged">
                                        <Binding.ValidationRules>
                                            <validation:DateRule/>
                                        </Binding.ValidationRules>
                                    </Binding>
                                </DatePicker.SelectedDate>
                            </DatePicker>
                            <ItemsControl Foreground="{DynamicResource MaterialDesignValidationErrorBrush}" FontSize="12"
                                        ItemsSource="{Binding ElementName=BirthdayDatePicker, Path=(Validation.Errors)}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding ErrorContent}" TextWrapping="Wrap" Margin="4,2,0,0"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>

                        
                        <StackPanel Margin="0,0,0,16">
                            <TextBlock Text="Краткое описание" FontSize="14" FontWeight="Medium" Margin="0,0,0,4"
                                     Foreground="{DynamicResource MaterialDesignBody}"/>
                            <TextBox x:Name="BioTextBox"
                                     materialDesign:HintAssist.Hint="О себе"
                                     AcceptsReturn="True"
                                     TextWrapping="Wrap"
                                     Height="60"
                                     VerticalScrollBarVisibility="Auto">
                                <TextBox.Text>
                                    <Binding Path="UserProfile.Bio" UpdateSourceTrigger="PropertyChanged">
                                        <Binding.ValidationRules>
                                            <validation:RequiredRule/>
                                        </Binding.ValidationRules>
                                    </Binding>
                                </TextBox.Text>
                            </TextBox>
                            <ItemsControl Foreground="{DynamicResource MaterialDesignValidationErrorBrush}" FontSize="12"
                                        ItemsSource="{Binding ElementName=BioTextBox, Path=(Validation.Errors)}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding ErrorContent}" TextWrapping="Wrap" Margin="4,2,0,0"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>

                        
                        <StackPanel Margin="0,0,0,16">
                            <TextBlock Text="Роль *" FontSize="14" FontWeight="Medium" Margin="0,0,0,4"
                                     Foreground="{DynamicResource MaterialDesignBody}"/>
                            <ComboBox x:Name="RoleComboBox"
                                      ItemsSource="{Binding Source={StaticResource rolesService}, Path=Roles}"
                                      DisplayMemberPath="Title"
                                      SelectedValue="{Binding UserProfile.RoleId}"
                                      SelectedValuePath="Id"
                                      materialDesign:HintAssist.Hint="Выберите роль"
                                      Height="36">
                                <ComboBox.Style>
                                    <Style TargetType="ComboBox">
                                        <Setter Property="materialDesign:HintAssist.IsFloating" Value="True"/>
                                    </Style>
                                </ComboBox.Style>
                            </ComboBox>
                            <ItemsControl Foreground="{DynamicResource MaterialDesignValidationErrorBrush}" FontSize="12"
                                        ItemsSource="{Binding ElementName=RoleComboBox, Path=(Validation.Errors)}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding ErrorContent}" TextWrapping="Wrap" Margin="4,2,0,0"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>

                    </StackPanel>
                </ScrollViewer>

                <Border Grid.Row="2" 
                        Background="{DynamicResource MaterialDesignToolBarBackground}"
                        Padding="24,16">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button x:Name="SaveButton" 
                                Click="save" 
                                Content="Сохранить в БД">
                            <Button.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="ContentSave" Width="16" Height="16" Margin="0,0,8,0"/>
                                        <TextBlock Text="Сохранить в БД" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </Button.ContentTemplate>
                        </Button>

                        <Button Click="back" 
                                Content="Назад"
                                Margin="16,0,0,0">
                            <Button.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="ArrowLeft" Width="16" Height="16" Margin="0,0,8,0"/>
                                        <TextBlock Text="Назад" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </Button.ContentTemplate>
                        </Button>
                    </StackPanel>
                </Border>
            </Grid>
        </materialDesign:Card>
    </Grid>
</Page>